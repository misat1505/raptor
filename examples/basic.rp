# import "vectors.rp";

i64 x = "";
i64 foo = 2 + 2;
foo = "Elon";
str a = foo;
str b = doesnt_exists;


i64 x = 5 as f64;

i64 a = 1;

if (true) {
  i64 x = 5;
} else {
  i64 a = 5;
}

for (i64 i = 0; a < 1; i = i + 1) {

}

switch (5: x) {
  (x < 0) -> {
  }
  (x == 0) -> {
  }
  (x > 0) -> {
  }
}

fn print_user(str name, i64 age): str {
  return name + " is " + age as str + " years old.";
}

str name = input("Input ur name: ");
println("Hello " + name + "!\n");

i64 age = input("Give us ur age: ") as i64;

str text = print_user(name, age);
println(text);

str[] names = [];
vector_push(&names, "Elon");

