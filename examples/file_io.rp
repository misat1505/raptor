str filepath = "foo.txt";

str text = "yes";
if (!exists_file(filepath)) {
  text = "no";
}
println(filepath + " exists: " + text);

write_file(filepath, "Hello world\n");

text = "yes";
if (!exists_file(filepath)) {
  text = "no";
}
println(filepath + " exists: " + text);
