# fn get_vector(): i64[][] {
#   return [[1, 2, 3], [4, 5, 6]];
# }

# i64[][] vec = get_vector();
# println(vector_stringify(vec));

# vec = [[1]];
# println(vector_stringify(vec));

# str[] names = ["Aboba", "foo"];
# println(vector_stringify(names));

# vector_push(&names, "Elon");
# println(vector_stringify(names));

# i64[] numbers = [];
# for (i64 i = 0; i < 10; i = i + 1) {
#   vector_push(&numbers, i);
# }
# println(vector_stringify(numbers));

fn append(&i64[] arr, i64 value): void {
  vector_push(&arr, value);
}

# append(&numbers, 420);
# println(vector_stringify(numbers));
